# PR #828：独立审查后的定点修复

> 📋 方案待拍板 · 状态由 docs-autosync 自动登记，作者请按实修改

状态：执行中，不是验收通过。沿用 `codex/core-a-integration-20260919`，保持草稿 PR，禁止合并。用户交付的独立报告审查 HEAD `04658ef65`；本轮先整合主线 #827，合并提交 `9cb822c7a`，保留原事务保护和主线仅新增内容才 fit 的行为。

## 范围与不变量

- IR-01：原确认入口批准后的 ×N、首帧依赖及批量继续写原项目；前台交互失效不能取消合法后台执行。原对象/内容/素材/身份真正变化仍拒绝旧授权。只改原 action → confirmation → runner → delivery 边界，不建新执行器或 context 管理器。
- IR-02：原分镜撤销只归属可见编辑器；Redo、已消费事件、输入控件和旁边工作区不可触发。撤销删除不能覆盖随后独立编辑或盲退画布历史。两 host 共用原入口。
- EV-01：真实 Electron 资源失败→局部重试→可编辑，画布/付款两宿主；原生窗口失焦/最小化与原 UI 中断组合分别记录，禁止把 dispatchEvent 当 OS 证据。资源图若使局部保护失效，先真实红测再修原共享 lazy owner。
- Ponytail：落实成立的机械精简，保留保护和不同输入覆盖。公共 schema/整份 IR 类型/测试框架替换不因减行自动批准；每条重新记录实际处置，不再用“冻结版本”统一否决。
- CI：本轮新查得 Unit 缺浏览器安装及 macOS 临时路径、Golden path 仍要求新建即落画布、性能旅程总预算耗尽。逐项归因后修原测试/安装边界，不能提高预算、削弱断言或改回错误产品行为。

范围仍是简化 A＋完整 T7，不扩 T3、不重画页面；#808 左栏行为及 T-DS-01/A-2 延期不变。既有真实付费产物仅 verify-only，不重复收费。

## 先查别人

- [原后台投递 owner](../../src/workbench/generationCanvas/runner/runProjectDelivery.ts) 已区分原项目 store 与磁盘；[原项目交互 owner](../../src/workbench/project/projectCanvasReadSurface.ts) 的 epoch 失效应保留。不能用后者替代持久执行身份。
- [原 Run host](../../src/workbench/creation/storyboard/useStoryboardRunHost.ts) 已按固定 project/run/document 读取并检查内容 token；[原画布快捷键](../../src/workbench/generationCanvas/components/useCanvasShortcuts.ts) 已有事件消费、可见性及输入归属规则。
- [原 lazy owner](../../src/ui/chunkBoundary.tsx) 已有局部重试；先查真实产物依赖和浏览器失败缓存，不用另写一套 factory 模拟恢复。
- [独立审查范围依据](../audit/evidence/core-a-20260920/scope/README.md) 与 [原重启方案](2026-09-20-core-a-restart.md) 是功能边界。此次使用既有内部机制，不新增第三方框架；对抗证据来自用户独立报告及原入口红测，不能声称做了新的外部研究。

## 分工与交接

root 亲读方案、原调用链和差异，独占整合、CI修复、精简批次及交付；IR-01 代理先门表/原入口红测，再负责 runner、row action、batch、repository/delivery；IR-02 代理先门表/原组件红测，再负责 editor 撤销及局部删除恢复。IR-01 若需 editor 的 runAction 修改，提交精确片段给 root，禁止与 IR-02 同文件并发编辑。EV-01 代理先资源图和真实资源红测，再经 root 核定最小生产修复；Electron 串行调度。子代理不提交、不推送。

六角色验收：架构查唯一 owner 与两种寿命；产品查批准后切项目不等于取消；设计查原页面/点击区域不变；前端查事件和异步闭包归属；后端查持久身份及授权不放宽；用户查从确认、切换到回看/重开的完整结果。收货者不能只复述实施者结论。

## 验收与回滚

用户最终收货标准（2026-09-20 再次确认）：

| 要求 | 收货标准 |
| --- | --- |
| 冻结简化 A＋完整 T7 | 不再造页面、执行系统或扩展 T3 |
| 修复已证执行与撤销问题 | 原入口先复现再修复；目标真正变化时仍安全拒绝 |
| 补真实调用链测试 | 确认后切项目、隐藏工作区 Undo／Redo、参数条真实资源加载失败恢复 |
| 按最终代码重新对账 | 每项结论对应具体提交、实际入口和证据，不以测试总数代替行为正确性 |

证据记录必须区分原组件受控测试、真实 Electron＋受控供应商、既有真实付费产物复核以及候选包；历史日志保留原提交/构建身份，不能直接改标为最终代码通过。

1. 先保存 IR-01/IR-02 原入口失败日志，增加真目标变化的负向用例；先合同/门表再改高风险路径。
2. 单元与受控原组件分别验证正反行为，随后真实 Electron 通过原确认、原快捷键、资源加载边界执行；截图 zh/en，并记录准确源码/构建身份。
3. 既有源码审计按 blob/patch 精确复用，变化项重新审；Ponytail、contracts、类型、build及风险相关系统测试绑定新 tree。全量测试/CI失败不得以定点绿取代。
4. 更新 #828 正文：旧报告保留，新发现逐条红绿/出处/限制，最终等外部复审，不合并。

回滚只撤本轮具体 patch，保留主线合入及原已验证功能；不整树回退、不覆盖共享文件。若出现需新状态正本、执行系统或改变产品交互的岔路，停止该方案并先核更小的原边界修复。

## 修后独立审查追加（尚未放行）

原入口复审又证实新输入校验漏掉两类：复制节点后等待授权期间才捕获输入，以及文本正文/模式/选区与批内手改正文未纳入实际输入校验。红证据 `/private/tmp/nomi-ir01-independent-probe-v3.log`，5 红/1 正向对照；文本正向替身仍需改用真实生成正文投影，不能把替身手写文档当原 append 行为。

最小边界仍是原确认与原正文落地：副本在授权等待前固定输入；文本首次执行校验真实正文、模式、选区。本批生成正文继续供下游使用，随后人工修改须拒绝。原 rewrite 通过现有 ProseMirror 选区应用，不能猜选区或重写编辑器。最终选择既有 run 作为唯一正文摘要 owner，沿用它的 runId/resultId 关联结果。原流式/定稿写口将正文与该 run 摘要一次写入，原选区结果应用也更新同一 run；普通编辑不更新摘要，不在 result 再存一份同义字段。摘要是结果绑定元数据，不保存第二份可编辑正文、不新增执行或上下文系统。原纯正文投影若需共享，仅抽无状态函数并删除旧实现。

已反查原付费守卫：`electron/spendGrant.ts` 负责 nodeIds、次数、有效期与报价额度，`spendConfirm.ts` 报价输入是模型和参数；它们没有封存正文或 prompt。原批准输入检查因此有独立职责，不能宣称主进程已有全文校验而删除；同时不复制令牌、报价和供应商提交机制。

另已复现后台进度投递未接拒绝：旧异步 I/O 漏接因本轮身份拒绝而确定可达。修在原运行 Promise 和批量 finally，保留拒绝、原项目任务失败可见与队列结算；被替换项目和当前项目均不得被旧回写覆盖。新增收尾回归不能借用此前全量单测的通过身份。

## 最终差异审计追加

- 文本同一 run 的重试：静态审查发现流式正文已由原 run 封存摘要，但执行节点的批准检查仍只接受初始正文；网络重试可能被误拒。先从原确认、generateText 流式写口及 runner 重试复现，保留人工编辑拒绝。若成立，复用已有 run 摘要判定同 run 的合法输出，并使每次文本重试沿用本次运行最初正文输入，避免把失败片段追加进下一次 prompt/正文；不新增持久化正文副本或执行器。
- T7 证据收尾：fixture 关闭后的零媒体/异常请求校验必须先于最终 report 写入；关闭阶段失败也写 failed。保留原断言，不以脚本退出码失败但 report passed 的矛盾证据放行。
- 原组框/多选拖动：租约中断清理遗漏了已应用位移的事件和持久化结算。原 hook 红测中，正常 pointerup 两条正控均移动并结算；blur/hidden/unmount 六条均移动但没有结算。恢复原共享拖动边界的合法收尾，原项目/图已替换时禁止写当前图；不撤除原舞台租约。首版夹具未形成有效组，正控失败，不能作为缺陷证据；有效红证据是 `/private/tmp/nomi-ev02-selection-drag-red-v2.log`。
- 后台旅程的项目 B 检查不得用读取异常回退空数组。结束必须成功读取完整 generationCanvas 并与切入 B 时的规范化 hash 相同；媒体 hash 单独保留。原脚本只数零节点的历史结果不能宣称完整画布未变。
- 全量 v4 与 contracts 被 root 并发启动，违反资源串行要求，保留失败记录。Undo 初始化 hook 超时与文本选区断言失败分别记录；不能把两者统称为启动超时。后续全量串行运行，不提高预算。
- 文本原组件探针证明：加载原 Tailwind 后，`Home` 未将光标移到正文开头；真实键盘全选后左移可建立起点，随后选中 `OLD`。仅测试增加原样式、正文/焦点/全选断言并替换定位操作，保留生成、持久化、重挂和人工编辑拒绝断言；没有更改产品编辑器。证据 `/private/tmp/nomi-pr828-text-selection-probe.json`，修后定点 `/private/tmp/nomi-pr828-text-selection-fixed.log`。

## 应用级复验发现

- 原分镜参考模型筛选：真实 Electron 首帧 v2 已通过原确认和后台投递，但原编辑器截图仍显示默认模型和通用参考入口。`useModelOptionsState('video')` 默认按 text_to_video 过滤，遗漏仅发布 image_to_video 的合法模型；图片 image_edit-only 同类受影响。两处筛选均在本轮基线中已存在。最小修复是在原 modelCatalogCache/useModelOptions 查询上显式支持 `any-published`，仅原编辑器请求该范围；保留默认单模式、availability、publishedModes 过滤和原控件。先让现有目录测试及真实分镜页的首帧槽断言失败，再修原边界；不得给测试模型补一个并不存在的文生模式来造绿。
- 同类核查延伸到原 Agent `presentStoryboard`：它也必须读取分镜可用的全部已发布模式，否则会漏判必需槽、漏投影新参考或保留旧首帧。沿用同一查询参数，仅改原两处 preload 调用。原调用链三项红绿分别覆盖缺首帧零确认/授权/执行、新首帧正确执行、旧节点保持 ID 且替换成指定首帧；不增加另一份目录、参考投影或生成入口。
- 原批量首帧资格：参考槽恢复显示后，截图暴露 `missingRequiredSlots` 未计入原 materializer 已支持的首帧依赖，导致“生成剩余”禁用。既有 helper 与原 batch 调用链红测、真实 Electron 禁用按钮证据均已保留。只在该共享判断中为非图片镜的启用首帧计一个 `first_frame` 来源，使用 `Math.max` 避免与显式绑定重复计数；尾帧、其他槽、禁用首帧及图片镜不获豁免。不改 runner、槽控件或生成方式。同一应用旅程改从原批量按钮发起，验证确认、实际首帧字节、视频、跨项目投递与磁盘；此前单镜证据保持单镜身份。

## 本轮验收约束补记

用户要求精简重复测试，补到真实改动边界。新增首帧守卫探针已由 6 项裁至 4 项，复用已有无 archetype 拒绝测试；目录替身下移至 IPC DTO，原 cache、映射、落地、确认和 runner 均参与。不同风险的低层负控不因数量目标删除。

- 选择器已拒绝空集合，不重复新增同样检查。选中后缺资源全跳过、预定试验未完成或仅一次成功，不得按全通过汇总；当前旅程按预期请求数、所有节点结果和完整磁盘状态逐项断言，尚不证明多轮稳定性。
- 程序契约、模块集成、真实 Electron、真实供应商及最终安装包分别记录。外部受控服务不能盖真实供应商绿灯，不向 store 注入成功结果代替旅程。
- 完整 T7 保留。合成事件、原生窗口事件和未覆盖 OS 组合分开记录，不降低正式安全设置。
- 不删关键断言、不跳过必测、不强制点击、不无依据加等待或自动接受视觉基线；定位器与预期变更均留失败证据及复核理由。
- 保留原编辑器、row actions 与 runner。新发现生产缺陷按原边界最小修复，独立记录差异；不建设另一套测试平台或执行系统。
- 本轮不新增真实 LLM、视觉评委或媒体付费调用。已有产物仅 verify-only；隔离目录不等于外网封锁，零媒体请求也不等于所有费用为零。

- T7 在构建 `c458c7b78e834f424de6985d62246957560f7325` 首项真实资源拦截中，工作台与节点保留，但局部重试未恢复。React.lazy 实例重建清不了 Chromium 150 的失败 module map；日志只有一次被拦截的资源请求。原受控工厂测试不能证明这一边界。
- 独立 Electron 探针确认：清 HTTP 缓存无效；失败后 fetch 成功也不能恢复原 import；modulepreload 失败同样被缓存。入口 URL 加查询参数能绕过入口失败并保留健康依赖单例，但依赖失败仍无法恢复，且对执行错误盲目重试会重复副作用。不引入全图 URL 改写或自制模块加载器，也不把失败注入改到 fetch 来制造通过。
- 当前 WHATWG 已要求移除失败加载记录；Chromium 在 [4f1a12d2](https://github.com/chromium/chromium/commit/4f1a12d2a79aa4e9446626056140a76c6dc5e0e2) 实现 `ModuleMapDoNotCacheFailedFetch`。本机 Electron 43.4.1 / Chromium 150.0.7871.224 和核查的稳定 Electron 44.4.3 / Chromium 152.0.7977.130 源码尚无该实现。此事实不授权本轮升级预发布内核；完整 T7 仍不通过。必须有保留草稿、不刷新应用、覆盖入口及依赖加载失败的真实证据后再放行。
- 原分块机制的独立实验已否决，裁决 `/private/tmp/nomi-ev02-topology-experiment/VERDICT.md`。把共享依赖与原 lazy host 归组后，composer 虽仍 lazy，却因传递依赖使首屏静态代码增加约 2.04 MB，共享资源失败扩大到启动边界；629 个非空模块提前执行。不进入生产，不把拓扑三项通过写成重试修复，不继续扩展成自研模块加载器。保留真实失败并继续按支持内核/原资源边界查找有证据的最小路径。
- Undo 首次系统复验已通过 legacy 即时撤销和隐藏工作区画布 Undo/Redo；脚本随后直接 focus 已自动收起的侧栏而超时。保留 #808 原行为，测试通过原按钮展开、验证侧栏隔离后收起，失败报告显式标 `failed`，不改产品、不放宽等待或断言。
